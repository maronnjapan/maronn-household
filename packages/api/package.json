{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:apply:local": "wrangler d1 migrations apply maronn-household --local",
    "db:seed:local": "wrangler d1 execute maronn-household --local --file=./seeds/seed.sql",
    "test": "vitest",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.3",
    "hono": "^3.12.8"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.11.1",
    "@cloudflare/workers-types": "^4.20240117.0",
    "drizzle-kit": "^0.20.11",
    "typescript": "^5.3.3",
    "ulidx": "^2.4.1",
    "vitest": "^3.2.4",
    "wrangler": "^3.24.0"
  }
}