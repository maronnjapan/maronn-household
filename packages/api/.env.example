# Cloudflare Workers の環境変数
# wrangler.toml で設定されていますが、必要に応じてここでも定義できます

# D1 Database
# ローカル開発時は wrangler dev コマンドで自動的にローカルDBが使用されます
# DATABASE_ID は wrangler.toml で設定してください

# Durable Objects
# ローカル開発時は wrangler dev で自動的に使用されます

# Environment
# ENVIRONMENT=development

# CORS Allowed Origins (本番環境のみ)
# ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
